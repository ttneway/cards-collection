# 校園集卡牌 Completed

仍待處理項目請見 [backlog.md](/D:/codex%20test/cards-collection/docs/backlog.md)。

### P3-25 成就圖片與成就生圖功能
- 狀態：已完成
- 完成內容：成就管理已支援圖片上傳、雲端 AI、共享 ComfyUI、預覽、套用與儲存；學生端成就卡也會顯示圖片。

### P3-26 角色稱位系統
- 狀態：已完成
- 完成內容：教師可建立、授與與移除稱位；學生保留歷史稱位名稱，僅目前稱位提供效果並顯示於角色頁。

### P3-28 收集冊封面與 AI 生成封面
- 狀態：已完成
- 完成內容：收集冊管理已支援封面上傳、雲端 AI、共享 ComfyUI、預覽及套用，前後台皆可顯示封面。

### P3-29 成就預設模板條件修正
- 狀態：已完成
- 完成內容：累積與連續成就已使用不同條件類型；預設草稿、簡單模式與進階模式皆可正確處理 cumulative 與 streak 規則。
### P3-27 角色照片與圖生圖 Q 版頭像
- 狀態：已完成
- 完成內容：教師可上傳已註冊學生的原始照片，使用共享 ComfyUI 生成 Q 版預覽後套用；原圖與生成頭像分開保存，角色頁優先顯示生成圖。

### P3-31 角色頁個人照片放大
- 狀態：已完成
- 完成內容：角色頁已將頭像調整為大型固定比例顯示，桌面與手機版皆保持不擠壓角色資訊。

### P3-32 卡牌主色改為可視化色彩選擇
- 狀態：已完成
- 完成內容：卡牌管理主色已改為色彩選擇器與色碼預覽，保留既有資料與 AI 提示詞相容性。
## 2026-07-21

### P3-30 職業效果依職業獨立累積
- 狀態：已完成
- 優先級：P3
- 完成內容：每個已解鎖職業現在會各自保存效果快照。切回舊職業時，會維持離開該職業時的效果數值，不再依角色總等級補算成長。
- 成長規則：角色升級時，系統只會增加當時主職業的每級效果；其他已解鎖職業的效果保留且仍有效，但不會在未裝備期間成長。
- 相容處理：既有玩家職業已依原本當下可見的效果建立初始快照，避免此次更新造成既有加成突然變動。

## 2026-07-10

### P3-23 教師後台說明頁
- 狀態：已完成
- 優先級：P3
- 完成內容：新增 `/teacher/help` 教師後台說明頁，集中整理卡牌、卡包、任務、學生、職業、裝備與 AI 生圖的用途、建議操作順序與注意事項。
- AI / Hugging Face 說明：頁面內補上 Hugging Face API key 申請方式、作者 / 模型欄位填寫格式、模型路徑範例，並在教師首頁與 AI 生圖區塊加入說明入口。

### P3-24 角色性別選擇與職業男女圖片
- 狀態：已完成
- 優先級：P3
- 完成內容：`profiles` 新增角色性別欄位，`profession_templates` 新增 `icon_url_male` / `icon_url_female`，學生可在 `/character` 切換男生 / 女生形象。
- 圖片顯示邏輯：前台職業圖片會依學生所選性別顯示對應職業圖，若缺少男女專用圖則自動回退到原本預設職業圖；教師職業後台也已可分別輸入與上傳男女職業圖片。

## 2026-07-03

### P3-17 共享 ComfyUI 主機版本地生圖
- 狀態：已完成
- 優先級：P3
- 完成內容：新增共享 ComfyUI 主機方案。教師後台已加入 `/teacher/ai-remote` 設定頁，可維護全校共用的 Gateway 公開網址、共享金鑰、ComfyUI API workflow JSON、負面提示詞與啟用狀態。卡牌管理頁已可切換 `雲端 AI` / `共享 ComfyUI 主機`。
- 生成流程：共享主機模式採「先產生預覽、再套用到卡牌」。教師確認預覽後，系統才會把圖片上傳到 `card-images` Storage 並回填卡牌圖片欄位。
- 技術實作：新增 `remote_ai_settings` 設定資料表與對應 RPC、共享提示詞 builder、Gateway 健康檢查與遠端預覽流程；專案內也提供 `tools/comfyui-shared-gateway` 範本，可部署在實際跑 ComfyUI 的那台電腦上。

### P3-12 職業與裝備平衡預覽工具
- 狀態：已完成
- 優先級：P3
- 完成內容：職業管理頁已整合 `preview_character_bonuses(...)` 預覽區，可用 10 / 20 / 30 / 40 / 50 / 60 級快速切換，立即查看目前職業設定在不同等級下的加成摘要與明細。

### P3-11 AI 生成職業 / 裝備圖片
- 狀態：已完成
- 優先級：P3
- 完成內容：職業管理與裝備管理頁皆已整合 AI 生圖流程，可直接在站內生成圖片，並回填對應欄位。職業使用 `icon_url`，裝備使用 `image_url`，圖片仍統一存入 Supabase Storage。

### P3-16 教師自行輸入 AI 圖片 API Key
- 狀態：已完成
- 優先級：P3
- 完成內容：教師卡牌管理頁新增「教師自備 API key」設定區，可選 Gemini 或 OpenAI / ChatGPT，並輸入本次生成要使用的 API key。`generate-card-image` Edge Function 已支援 request payload 內的 `aiProvider` 與 `apiKey`，生成時優先使用教師自備 key；欄位留空時會退回使用 Supabase Edge Function Secrets 中的系統 key。
- 安全設計：教師輸入的 API key 只暫存在目前頁面狀態，不寫入資料庫，也不寫入瀏覽器永久儲存。頁面文案會明確提示 key 會在生成時送到 Edge Function 呼叫圖片 API。
- 介面行為：AI 設定狀態會顯示目前使用的 provider、model 與 key 來源（教師自備 key 或系統 Secret）。缺系統 secret 時，教師只要在頁面輸入自己的 key 仍可啟用生成按鈕。

## 2026-06-29

### P3-22 卡牌與裝備圖片上傳建立
- 狀態：已完成
- 優先級：P3
- 完成內容：卡牌與裝備管理頁已支援直接上傳本機圖片，系統會自動上傳到 Supabase Storage 並回填圖片網址，無需再手動貼外部連結。

### P3-21 分集冊與卡牌管理拆頁
- 狀態：已完成
- 優先級：P3
- 完成內容：原本混在同頁的分集冊設定與卡牌管理已拆開，改為 `/teacher/card-albums` 與 `/teacher/cards`，並加入頁面切換入口，讓操作焦點更清楚。

### P3-20 卡牌建立流程不中斷
- 狀態：已完成
- 優先級：P3
- 完成內容：建立新卡後表單會自動回到可繼續新增下一張的狀態，不會卡在編輯模式，方便教師連續建立多張卡片。

### P3-19 卡牌清單縮放滑桿
- 狀態：已完成
- 優先級：P3
- 完成內容：`卡牌圖鑑`、`我的牌庫` 與教師卡牌管理頁都已加入卡牌大小縮放滑桿，並使用 localStorage 記住顯示偏好。

### P3-18 卡包抽卡張數與連抽機制
- 狀態：已完成
- 優先級：P3
- 完成內容：卡包管理頁已可設定每次抽出張數；前台抽卡已支援 1 抽、10 連抽、20 連抽與連續抽卡，並補上連續抽卡時可即時停止的操作。

### P3-15 免費額度 AI 卡圖服務串接
- 狀態：已完成
- 優先級：P3
- 完成內容：查核目前官方圖片 API 後，Gemini 圖片模型與 OpenAI 圖片模型都不是穩定的永久免費 API；因此改成可切換供應商架構。`generate-card-image` Edge Function 會依 `AI_IMAGE_PROVIDER` 指定或自動偵測 `GEMINI_API_KEY` / `OPENAI_API_KEY`，預設優先使用 Gemini，OpenAI 預設模型改為較低成本的 `gpt-image-1-mini`。教師卡牌管理頁新增 AI 圖片設定狀態檢查，可直接看到目前 provider、model 與缺少的 Supabase Secret。
- 設定方式：在 Supabase Edge Function Secrets 設定 `AI_IMAGE_PROVIDER=gemini` 搭配 `GEMINI_API_KEY`，或設定 `AI_IMAGE_PROVIDER=openai` 搭配 `OPENAI_API_KEY`。可選擇設定 `GEMINI_IMAGE_MODEL`、`OPENAI_IMAGE_MODEL` 覆寫預設模型。
- 備註：目前尚未放入任何真實 API key；需要由專案持有人在 Supabase 後台設定 secret 後再實機生成。

### P3-14 遊戲說明 / 規則頁面
- 狀態：已完成
- 優先級：P3
- 完成內容：新增前台遊戲說明頁，整理核心玩法、任務規則、等級公式、Lv.1-Lv.10 門檻、職業解鎖、裝備欄位、抽卡與公告機制，並加入全站導覽入口。

### P3-13 抽卡卡包設定管理頁面
- 狀態：已完成
- 優先級：P3
- 完成內容：新增教師卡包管理頁，可建立 / 編輯卡包、批次加入卡片、設定各卡權重、切換啟用狀態，並即時查看各稀有度機率。

### P3-10 AI 生成卡牌圖片
- 狀態：已完成（需設定 `OPENAI_API_KEY` 才能正式生成）
- 優先級：P3
- 完成內容：卡牌管理頁已支援 AI 風格模板、提示詞補充、建立並生成卡圖；新增 Supabase Edge Function `generate-card-image` 與 `card-images` Storage bucket，生成後會自動回填卡片圖片。

### P3-3 QR Code 辨識穩定性
- 狀態：已完成
- 優先級：P3
- 完成內容：依目前實機測試結果，現行 QR Code 皆可正常辨識，這項目先結案，不再列入優先待辦。

### P2-6 任務期限與過期隱藏
- 狀態：已完成
- 優先級：P2
- 完成內容：任務新增開始時間、結束時間、到期後保留顯示天數；學生前台與掃碼工作站都會依當前時間自動隱藏不該出現的任務。

### P2-7 教師任務統計分析儀表板
- 狀態：已完成
- 優先級：P2
- 完成內容：新增教師任務統計頁，提供時間區間篩選、班級排行榜、每日趨勢、任務執行情況、學生排行與個人 CSV 報表匯出。

### P2-10 角色等級與累積總獲點系統
- 狀態：已完成
- 優先級：P2
- 完成內容：新增 `player_progress`，以累積獲點計算等級，並回填既有歷史 `earn` 交易。

### P2-11 職業系統與 10 級轉職節點
- 狀態：已完成
- 優先級：P2
- 完成內容：新增職業模板、職業效果、10 / 20 / 30+ 職業池與預設 9 個職業。

### P2-12 多職業保留效果與主職切換
- 狀態：已完成
- 優先級：P2
- 完成內容：支援舊主職效果凍結保留、主職切換、已解鎖職業之間自由切換。

### P2-13 角色頁與能力總覽介面
- 狀態：已完成
- 優先級：P2
- 完成內容：新增 `/character` 頁面，顯示等級、主職、已解鎖職業、裝備與最終能力摘要。

### P2-14 裝備模板、背包與四格穿戴
- 狀態：已完成
- 優先級：P2
- 完成內容：新增裝備模板、玩家背包、四格穿戴、角色商店與購買裝備流程。

### P2-15 教師職業管理後台
- 狀態：已完成
- 優先級：P2
- 完成內容：新增 `/teacher/professions`，可建立 / 編輯職業模板與能力效果，並顯示平衡提醒。

### P2-16 教師裝備管理與發放後台
- 狀態：已完成
- 優先級：P2
- 完成內容：新增 `/teacher/equipment`，可建立裝備、設定商店售價、並直接發放給學生。

### P2-17 任務 / 抽卡 / 商店接入角色能力
- 狀態：已完成
- 優先級：P2
- 完成內容：任務加點、掃碼任務、卡包折扣、SSR / UR 抽卡率與裝備商店已接入角色能力加成。

## 先前完成

### P1
- `P1-1` 週期性任務異常
- `P1-2` 掃碼免登入完成任務
- `P1-3` 多方式登入
- `P1-4` 教師建立學生帳號可登入
- `P1-5` 註冊帳號歸類與身分碼規則
- `P1-6` 註冊帳號補齊 QR Code / 條碼

### P2
- `P2-1` 任務編輯與刪除
- `P2-2` 任務下次可取得間隔時間
- `P2-3` 管理者角色
- `P2-4` 教師後台卡牌管理
- `P2-5` 任務班級 / 全校範圍
- `P2-8` 掃碼發點工作站免滑鼠連續掃描
- `P2-9` 任務開啟者角色限制

### P3
- `P3-1` 教師後台成就管理
- `P3-2` 卡牌收集冊
- `P3-4` 高稀有抽卡跑馬燈
- `P3-5` 系統公告 / 任務自動公告
- `P3-6` 稀有度中文與英文代號
- `P3-7` SSR / UR 抽卡機率公告
- `P3-8` 個人首頁最新公告
- `P3-9` 教師自建分集冊
